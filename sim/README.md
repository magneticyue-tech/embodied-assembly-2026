**算法**:
- `vision.py`: OpenCV 管线 (BGR->HSV 分割 -> 形态学 -> 轮廓 -> minAreaRect -> 单应反投影到台面 mm)
- **旋转鲁棒**: 方块/托盘可旋转, 用 minAreaRect 取中心+角度 (非外接正框); 正方形 90° 对称折叠到 [-45,45]
- **整块刚性托盘板 + 遮挡鲁棒** (本版重点):
  - 托盘是**一整块矩形板**, 板上 6 个同心环色槽按固定布局排布, 共享板的位姿。
  - **颜色→槽位映射**: 开局无遮挡时**一次性标定**, 之后固定不变。
  - **板位姿**: 每步靠**板外轮廓矩形**重新检测 (整块板永不被小方块遮挡; 中途裁判挪板也能跟上)。
  - 装配时**目标槽位置 = 板位姿 ⊕ 标定布局**, 不靠重新识别颜色 ——
    所以方块放上去盖住槽色、或放置方块与其它槽同色, 都不会造成混淆。
- **同心环精度尺**: 槽按 troy.png 渲染 (每环 1mm, 每 5mm 加粗, ±20mm), 放置偏移按"落在第几环"读出
- `scene.perturb()`: 每步前模拟裁判挪动/转动自由方块 **+ 整块托盘板**
- 逐步高亮当前方块(红框)与目标槽(黄框)

## 运行

```bash
cd sim
python main.py
```
依赖: `numpy opencv-python pillow`。
 `python -m pip install numpy opencv-python pillow`

## 产物 (output/)

| 文件 | 内容 |
|---|---|
| `00_initial_scene.png` | 初始场景: 自由方块 + 整块托盘板(外轮廓框)+ 6 色槽标定 |
| `task1_scene.png` | 任务一: 方块旋转框+坐标、托盘板外轮廓、各色槽标注 |
| `task2_step{1-6}_*.png` | 任务二每步: 已放置方块盖住槽色、板位姿重检测、当前步骤高亮 |
| `parse_log_20260617.txt` | 带时间戳全流程日志 (满足赛题强制要求), 含每步"放置偏移→第几环"评分 |

## 模块对应正向设计方案

| 文件 | 对应方案章节 |
|---|---|
| `config.py` | 坐标系约定 / 硬件参数 (AUBO-i5、同心环托盘) |
| `scene.py` `camera.py` | 仿真台面与相机采集 (旋转方块 + 同心环托盘) |
| `vision.py` | §3 视觉模块 (旋转鲁棒分割 + 反投影) |
| `cognition.py` | §5 大模型智能体 (结构化 JSON + 置信度校验) |
| `execution.py` | §6 执行层 (AUBO-i5 抓放 + 同心环误差读数) |
| `main.py` | §6.4 双任务状态机 + 逐步可观测 |

